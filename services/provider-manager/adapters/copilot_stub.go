package main

import (
	"context"
	"fmt"
)

// copilot stub adapter - placeholder for Copilot API integration
func callCopilotRaw(ctx context.Context, prompt string, model string) (string, error) {
	// For now return an error indicating not configured
	return "", fmt.Errorf("copilot adapter not configured")
}

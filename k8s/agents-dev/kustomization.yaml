apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: agents-dev
resources:
  - ../base
  - ../../service-account.yaml
  - orchestrator-deployment.yaml
  - worker-deployment.yaml
  - provider-manager-deployment.yaml

commonLabels:
  app: agents-platform
